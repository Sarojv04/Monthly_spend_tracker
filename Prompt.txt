------------------------------------------terms.html Prompt------------------------------------------------
Add a new route in app.py:
GET /terms → renders templates/terms.html

Create templates/terms.html with generic terms and conditions content appropriate for a personal expense tracking app.
Include sections like:

Acceptance of Terms
Use of Service
User Data
Limitations of Liability
Changes to Terms

Extend base.html if it exists, otherwise match the style of landing.html.

In templates/landing.html, update the "Terms and Conditions" footer link href from "#" to "/terms".
Also make sure the appearance of @templates/terms.html matches the website's theme

------------------------------------------- Privacy Policy ------------------------------------------------

Do the same as you did for the Terms and Conditions page, but for Privacy Policy.

Add a new route in app.py:
GET /privacy → renders templates/privacy.html

Create templates/privacy.html with generic privacy policy content appropriate for a personal expense tracking app.
Include sections like:

Data We Collect
How We Use Your Data
Data Storage
Third Party Services
Contact Us

Match the style of terms.html.

In templates/landing.html, update the "Privacy Policy" footer link href from "#" to "/privacy".
Make sure the appearance looks like the website's theme

------------------------------- hero.png-------------------------------------------------------

Use the image located at static/images/hero.png as reference.
Update the hero section in @templates/landing.html and @static/css/landing.css to match it.

------------------------------------ See how it works -----------------------------------------

Add a modal to @templates/landing.html that opens when the user clicks "See how it works".

Requirements:

Clicking "See how it works" opens a modal overlay
Modal contains an embedded YouTube video (use any placeholder YouTube URL for now, I will replace it later)
Video should be playable inside the modal
Clicking the close button OR clicking outside the modal closes it

When the modal closes, the video must stop playing (not continue in background)

No page libraries or dependencies — vanilla JS only, since we are not using any JS framework in this project

Do not modify any other part of the page.

--------------------------------------------- See how it works -> changing style of the button ----------------------------------

Modify only the "See how it works" button in @templates/landing.html and @static/css/landing.css to match the exact same style as the "Create free account" button.

Requirements:

"See how it works" button must have identical styling as "Create free account" (background color, text color, padding, border radius, font, hover effects, etc.)
Keep the text "See how it works" unchanged
Maintain proper spacing between the two buttons
Do not change functionality or links of either button

Do not modify any other part of the page

-------------------------------------------- Plan Mode creation----------------------------------------------------------------------------
Enter Plan mode and create a plan based on the spec document

Read .claude/specs/01-database-setup.md and the existing database/db.py and app.py, then generate an implementation plan. Save this plan to .claude/plans/01-database-setup.md

----------------------------------------------------seed user prompt ------------------------------------------------------------

---
description: Create a single dummy user in the database
allowed-tools: Read, Bash(python:*)
---

Read database/db.py to understand the users table schema and the get_db() helper.

Then write and run a Python script using Bash that:

1. Generates a realistic random  user using your own knowledge of common names across regions of the world:
   - Name: a realistic Indian first + last name
   - Email: derived from the name with a random 2–3 digit number suffix (e.g. rahul.sharma91@gmail.com)
   - Password: "password123" hashed with werkzeug's generate_password_hash
   - created_at: current datetime

2. Checks if the generated email already exists in the users table. If it does, regenerate until unique.

3. Inserts the user into the database using the same get_db() pattern found in db.py.

4. Prints confirmation:
   - id
   - name
   - email